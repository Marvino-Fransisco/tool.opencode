version: '3.8'

services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes:
      - .:/workspace
    command: sleep infinity

  prod:
    image: skill/matplotlib
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    profiles:
      - prod